// app/components/SectionDescription/SectionDescription.tsx
import { SectionDescription } from "./styles";

export default function SectionDescriptionComponent() {
  return (
    <SectionDescription>
      <h4>As software developers we strive to deliver not just well-written code, but code that delivers real value to every customer.</h4>
    </SectionDescription>
  );
}